% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_CERIS.R
\name{run_CERIS}
\alias{run_CERIS}
\title{CERIS Exhaustive Search for Critical Environmental Windows}
\usage{
run_CERIS(
  env_mean_trait,
  env_params,
  params,
  max_days = NULL,
  loo = FALSE,
  loo_summary = median,
  progress = NULL
)
}
\arguments{
\item{env_mean_trait}{Data.frame with env_code and meanY columns}

\item{env_params}{Data.frame with env_code, DAP, and parameter columns}

\item{params}{Character vector of environmental parameter column names}

\item{max_days}{Maximum number of days after planting to search (default: max DAP)}

\item{loo}{Logical; if TRUE, perform leave-one-environment-out cross-validation
(default FALSE)}

\item{loo_summary}{Function used to summarize LOO correlations across
environments. Default is \code{median} (robust to outlier environments,
as in the original CERIS implementation). Use \code{mean} to compare.}

\item{progress}{Optional callback function receiving a fraction (0--1) for
progress reporting. Called every 100 windows.}
}
\value{
Data.frame with columns: Day_x, Day_y, window, midXY, R_<param>,
  P_<param> for each parameter
}
\description{
Searches all possible date windows for correlations between environmental
parameters and trait means across environments.
}
\examples{
\donttest{
d <- load_crop_data("sorghum")
exp_trait <- prepare_trait_data(d$traits, "FTdap")
env_mean_trait <- compute_env_means(exp_trait, d$env_meta)
params <- c("DL", "GDD", "PTT", "PTR", "PTS")
result <- run_CERIS(env_mean_trait, d$env_params, params, max_days = 80)
head(result)
}
}
