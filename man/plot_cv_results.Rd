% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cv_results.R
\name{plot_cv_results}
\alias{plot_cv_results}
\title{Plot Cross-Validation Results}
\usage{
plot_cv_results(
  cv_results,
  labels = paste0("1-to-", seq_along(cv_results) + 1),
  env_colors = NULL
)
}
\arguments{
\item{cv_results}{List of data.frames, each with Yprd, Yobs, env_code, Rep columns.
Typically: list(cv_env_result, cv_genotype_result, cv_combined_result)}

\item{labels}{Character vector of labels for each CV scenario
(default: "1-to-2", "1-to-3", "1-to-4")}

\item{env_colors}{Optional named character vector of colors per env_code}
}
\value{
A patchwork object
}
\description{
Multi-panel predicted vs observed scatter plots for different CV scenarios.
}
\examples{
\donttest{
d <- load_crop_data("sorghum")
exp_trait <- prepare_trait_data(d$traits, "FTdap")
env_mean_trait <- compute_env_means(exp_trait, d$env_meta)
params <- c("DL", "GDD", "PTT", "PTR", "PTS")
env_mean_trait <- compute_window_params(env_mean_trait, d$env_params, 20, 60, params)
cv_e <- cv_env(env_mean_trait, exp_trait)
plot_cv_results(list(cv_e), labels = c("1-to-2 (Env)"))
}
}
