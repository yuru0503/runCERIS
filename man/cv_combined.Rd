% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_combined.R
\name{cv_combined}
\alias{cv_combined}
\title{Combined Environment and Genotype Cross-Validation (1-to-4)}
\usage{
cv_combined(
  gFold,
  gIteration,
  SNPs,
  env_mean_trait,
  exp_trait,
  gp_method = c("rrBLUP", "BayesB"),
  nIter = 5000,
  burnIn = 1000,
  seed = NULL,
  progress = NULL
)
}
\arguments{
\item{gFold}{Number of CV folds}

\item{gIteration}{Number of iterations (reshuffles)}

\item{SNPs}{Genotype matrix (line_code as rownames)}

\item{env_mean_trait}{Data.frame with env_code, meanY, kPara}

\item{exp_trait}{Data.frame with line_code, env_code, Yobs}

\item{gp_method}{Genomic prediction method: \code{"rrBLUP"} (default) or
\code{"BayesB"}.}

\item{nIter}{Integer. MCMC iterations for BayesB (default 5000).}

\item{burnIn}{Integer. Burn-in iterations for BayesB (default 1000).}

\item{seed}{Integer or \code{NULL}. Random seed for reproducible results.}

\item{progress}{Optional callback function(fraction)}
}
\value{
Data.frame with columns: line_code, env_code, Yprd, Yobs, Rep
}
\description{
Combines leave-one-environment-out with K-fold genotype cross-validation.
For each dropped environment, recalculates reaction norms and then performs
genotype CV with GBLUP (rrBLUP) or BayesB (BGLR).
}
\examples{
\donttest{
d <- load_crop_data("sorghum")
exp_trait <- prepare_trait_data(d$traits, "FTdap")
env_mean_trait <- compute_env_means(exp_trait, d$env_meta)
params <- c("DL", "GDD", "PTT", "PTR", "PTS")
env_mean_trait <- compute_window_params(env_mean_trait, d$env_params, 20, 60, params)
SNPs <- prepare_genotype(d$genotype, unique(exp_trait$line_code))
cv_result <- cv_combined(gFold = 5, gIteration = 2, SNPs = SNPs,
                         env_mean_trait = env_mean_trait, exp_trait = exp_trait)
head(cv_result)
}
}
