% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jgra_marker.R
\name{jgra_marker}
\alias{jgra_marker}
\title{Joint Genomic Regression Analysis (Marker Effects)}
\usage{
jgra_marker(
  pheno,
  geno,
  envir,
  enp,
  env_meta,
  tt_line = NULL,
  tt_env = NULL,
  method = c("RM.E", "RM.G", "RM.GE"),
  gp_method = c("rrBLUP", "BayesB"),
  nIter = 5000,
  burnIn = 1000,
  seed = NULL,
  fold = 10,
  reshuffle = 5,
  progress = NULL
)
}
\arguments{
\item{pheno}{Wide-format phenotype data.frame (line_code + environment columns)}

\item{geno}{Genotype data.frame or matrix with line_code identifier}

\item{envir}{Data.frame with env_code and environmental parameter columns}

\item{enp}{Name or index of the environmental parameter column to use}

\item{env_meta}{Data.frame with env_code (used for filtering)}

\item{tt_line}{Minimum non-NA lines per environment}

\item{tt_env}{Minimum non-NA environments per line}

\item{method}{One of "RM.E" (env LOO), "RM.G" (genotype CV), "RM.GE" (combined)}

\item{gp_method}{Genomic prediction method: \code{"rrBLUP"} (default, ridge
regression BLUP) or \code{"BayesB"} (Bayesian variable selection via BGLR).}

\item{nIter}{Integer. MCMC iterations for BayesB (default 5000). Ignored
when \code{gp_method = "rrBLUP"}.}

\item{burnIn}{Integer. Burn-in iterations for BayesB (default 1000). Ignored
when \code{gp_method = "rrBLUP"}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducible results.}

\item{fold}{Number of CV folds (default 10)}

\item{reshuffle}{Number of iterations (default 5)}

\item{progress}{Optional callback function(fraction)}
}
\value{
A list with:
  \item{predictions}{Data.frame with obs, pre, envir columns}
  \item{r_within}{Within-environment correlations}
  \item{r_across}{Across-environment correlation(s)}
}
\description{
Performs genomic prediction through estimation of environment-specific marker
effects and predicting their values in new environments using GBLUP (rrBLUP)
or BayesB (BGLR).
}
\examples{
\dontrun{
d <- load_crop_data("sorghum")
exp_trait <- prepare_trait_data(d$traits, "FTdap")
env_mean_trait <- compute_env_means(exp_trait, d$env_meta)
params <- c("DL", "GDD", "PTT", "PTR", "PTS")
env_mean_trait <- compute_window_params(env_mean_trait, d$env_params, 20, 60, params)
pheno <- prepare_line_by_env(exp_trait, env_mean_trait)
SNPs <- prepare_genotype(d$genotype, unique(exp_trait$line_code))
geno_df <- data.frame(line_code = rownames(SNPs), SNPs, check.names = FALSE)
envir <- env_mean_trait[, c("env_code", "kPara")]
result <- jgra_marker(pheno, geno_df, envir, "kPara", d$env_meta,
                      method = "RM.E", fold = 5, reshuffle = 2)
result$r_across
}
}
